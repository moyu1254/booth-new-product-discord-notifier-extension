# BOOTH New Product Discord Notifier Extension v0.3.4

設定画面から手動実行したときに、実行時間が長いとメッセージチャネルが閉じてエラーになる問題を修正したリリースです。

## 変更内容

- `今すぐ実行` は実行完了ではなく実行開始を待つ方式に変更
- 長時間の検索や通知処理でも `sendMessage` の応答チャネル切断エラーが出ないよう修正
- 手動実行開始時の設定画面メッセージを実際の動作に合わせて更新
